#include "PropControl.h"

namespace PlayfulTechnology {
	
	
	//command PropControl::commandList[32] = {};
	//byte PropControl::numRegisteredCommands = 0;
	/*
	void registerCommand(command c) {
				commandList[numRegisteredCommands] = c;
				numRegisteredCommands++;
			}	
	*/
	
	/*
	void PropControl::setClient(PubSubClient& client){
	//	this->_client = &client;
	}
	
	void PropControl::setServer(IPAddress ip, uint16_t port) {
	//	this->_client->setServer(domain, port);
	}
	*/
}